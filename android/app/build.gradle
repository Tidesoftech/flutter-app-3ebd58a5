android {
    namespace "pats"
    compileSdkVersion 34

    defaultConfig {
        applicationId "pats"
        minSdkVersion 21
        targetSdkVersion 34
        versionCode 1
        versionName "1.0.0"
    }

    buildTypes {
        release {
            signingConfig signingConfigs.debug
        }
    }
}

flutter {
    source '../..'
}